function getDefaultParams() {
	var params = {
		'client_type': 'openapi',
		'program_type': 'normal',
		'api_version': 3
	};
	return params;
}

// 验证必须的参数
// 所有方法必须用的
function checkParams(httpParams) {

}

exports.getDefaultParams = getDefaultParams;